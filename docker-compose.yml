version: "3.0"
services:
  daemon:
    build: .
    depends_on:
      - database
    expose:
      - 8080
  database:
    image: mongo:latest
    volumes:
      - /opt/spaceapidaemon/db:/data/db
  swagger:
    image: swaggerapi/swagger-ui
    depends_on:
      - daemon
    expose:
      - 8080
    enviroment:
      - API_URL=http://localhost/swagger.json
  reverse_proxy:
    build: reverseProxy
    depends_on:
      - daemon
      - swagger
    ports:
      - 80:80